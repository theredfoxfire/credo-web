{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>History</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ history.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ history.title }}</td>
            </tr>
            <tr>
                <th>Story</th>
                <td>{{ history.story }}</td>
            </tr>
            <tr>
                <th>Largeimage</th>
                <td>{{ history.largeImage }}</td>
            </tr>
            <tr>
                <th>Smallimage</th>
                <td>{{ history.smallImage }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('history_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('history_edit', { 'id': history.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
