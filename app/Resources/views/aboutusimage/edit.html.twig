{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Logo edit</h1>
    <a href="{{ path('logo_index') }}">Back to the list</a> <br /><br />
    <img src="{{ asset('uploads/logo/') }}{{ logo.largeImage }}" height="100" width="100">


    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}<br />
        <input type="submit" value="Edit" class="btn btn-default"/>
    {{ form_end(edit_form) }}

{% endblock %}
