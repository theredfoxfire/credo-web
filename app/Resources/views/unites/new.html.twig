{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Unites creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}<br />
        <input type="submit" class="btn btn-default" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('unites_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
