{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Branches</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ branch.id }}</td>
            </tr>
            <tr>
                <th>Branchename</th>
                <td>{{ branch.brancheName }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ branch.address }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ branch.phone }}</td>
            </tr>
            <tr>
                <th>Facisimile</th>
                <td>{{ branch.facisimile }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('branches_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('branches_edit', { 'id': branch.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
