{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Quote edit</h1>
    <a href="{{ path('quote_index') }}">Back to the list</a> <br /><br />
    {{ quote.quote }}


    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}<br />
        <input type="submit" value="Edit" class="btn btn-default"/>
    {{ form_end(edit_form) }}

{% endblock %}
