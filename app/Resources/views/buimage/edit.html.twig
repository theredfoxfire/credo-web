{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Business Units Image  edit</h1>
    <a href="{{ path('buimage_index') }}">Back to the list</a> <br /><br />
    <img src="{{ asset('uploads/buimage/') }}{{ buimage.largeImage }}" height="100" width="100">


    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}<br />
        <input type="submit" value="Edit" class="btn btn-default"/>
    {{ form_end(edit_form) }}

{% endblock %}
