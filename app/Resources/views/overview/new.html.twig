{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Overview creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}<br />
        <input type="submit" class="btn btn-default" value="Create" />
    {{ form_end(form) }}

{% endblock %}
